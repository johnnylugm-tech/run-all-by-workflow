# TEST_SPEC.md — Named Test-Case Catalog (Single Source of Truth) for taskq

> **Phase 2 deliverable.** Generated by Agent A (ARCHITECT) following the
> `derive_test_cases.md` skill protocol. Test names from P1 `TEST_INVENTORY.yaml`
> (root-level) are preserved verbatim where specified — the 63 tc_ids enumerated
> in `TRACEABILITY_MATRIX.md` §5 each become one row in this catalog. This is
> the SINGLE SOURCE OF TRUTH for all test traceability checks at Gates 1-4
> (v2.6 unified D4). Validated by Agent B (TECH_LEAD) before P2 exit. P3 Agent A
> implements tests FROM this catalog — not ad-hoc.

**Project**: taskq
**SRS version**: `01-requirements/SRS.md` v1.0 (2026-07-28)
**SAD version**: `02-architecture/SAD.md` v1.0.0 (2026-07-28)
**TEST_INVENTORY.yaml version**: root `TEST_INVENTORY.yaml` v1.1 (2026-07-28)
**Generated**: 2026-07-28
**Active NFR Patterns**: NP-04 (validation 422) — forced by FR-01 injection/length/name rules
                        NP-06 (latency SLA) — forced by NFR-01 / NFR-09 p95
                        NP-07 (dependency fault) — forced by SAD: `taskq.store.atomic_write` (cache.json writes, NFR-07 fault scenarios)
                        NP-13 (concurrency) — forced by SAD: shared `taskq.store` Lock + `cache.json` / `tasks.json` writes
                        NP-15 (timeout) — forced by SAD: `taskq.executor` `subprocess.run(timeout=...)`

---

## NFR Pattern Activation (Step 1 + Step 1b Output)

The table enumerates all 15 patterns (NP-01…NP-15). The Trigger column records
the source: "SRS: <keyword>" (Step 1), "SAD: <module>" (Step 1b), or
"SEC: <threat-id>" (Step 1c). Step 1b's integration variants are forced under
`tests/integration/` and tagged `SAD:` in the case rows below; the case name
in the threat's `verified_by` is its own verification (Step 1c — no separate
case authored).

| Pattern | Trigger (SRS keyword / SAD module / SEC threat) | Activated |
|---------|--------------------------------------------------|-----------|
| NP-01 (auth 401)            | not found                                                              | ❌ |
| NP-02 (authz 403)           | not found                                                              | ❌ |
| NP-03 (rate limit 429)      | not found                                                              | ❌ |
| NP-04 (validation 422)      | SRS: "input validation"; FR-01 blacklist + length + name-uniqueness    | ✅ |
| NP-05 (idempotency)         | not found                                                              | ❌ |
| NP-06 (latency SLA)         | SRS: "p95"; NFR-01 50ms / 100 iters; NFR-09 100ms / 1000 tasks         | ✅ |
| NP-07 (dependency fault)    | SAD: `taskq.store.atomic_write` (cache.json + tasks.json + breaker.json); SEC: T-05 cross-process | ✅ |
| NP-08 (security attack)     | SEC: T-01 (injection), T-02 (shell=True regression), T-04 (secret leak) | ✅ |
| NP-09 (audit log)           | not found                                                              | ❌ |
| NP-10 (data round-trip)     | not applicable — store is write-once for state transitions, not round-trip | ❌ |
| NP-11 (backward compat)     | not found                                                              | ❌ |
| NP-12 (pagination)          | not found                                                              | ❌ |
| NP-13 (concurrency)         | SAD: shared `taskq.store.Lock` + cache.json concurrent readers/writers; SAD: `taskq.breaker` cross-process state | ✅ |
| NP-14 (encryption)          | not found (secrets are redacted not encrypted)                          | ❌ |
| NP-15 (timeout)             | SAD: `taskq.executor` `subprocess.run(timeout=TASKQ_TASK_TIMEOUT)`; SEC: T-03 hang | ✅ |

**Architecture-risk forcing** (Step 1b): every SAD-forced case is placed in
`tests/integration/` and tagged `SAD:` in the `Derivation` column below.

---

## Functional Requirement Test Cases

### FR-01: Submit and validate tasks

**Classification**: API_ENDPOINT
**Active Patterns**: NP-04 (validation), NP-07 (atomic write boundary), NP-13 (cross-process store lock)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr01_01_empty_whitespace_rejection` | command=""; expected_exit="2"; scenario="empty string" | happy_path | Q2 (validation) |
| 2 | `test_fr01_01_empty_whitespace_rejection` | command="   "; expected_exit="2"; scenario="all whitespace" | validation | Q2 (whitespace) |
| 3 | `test_fr01_02_length_rejection` | command_repeat="x"; command_length="1001"; expected_exit="2" | validation | Q3 (boundary >1000) |
| 4 | `test_fr01_03_injection_blacklist` | command="echo hi ; ls"; expected_exit="2"; blacklisted=";" | validation | Q2 (injection ;); NP-04; SAD: cli |
| 5 | `test_fr01_03_injection_blacklist` | command="echo hi \| wc"; expected_exit="2"; blacklisted="\|" | validation | Q2 (injection \|); NP-04; SAD: cli |
| 6 | `test_fr01_03_injection_blacklist` | command="echo hi & bg"; expected_exit="2"; blacklisted="&" | validation | Q2 (injection &); NP-04; SAD: cli |
| 7 | `test_fr01_03_injection_blacklist` | command="echo $HOME"; expected_exit="2"; blacklisted="$" | validation | Q2 (injection $); NP-04; SAD: cli |
| 8 | `test_fr01_03_injection_blacklist` | command="cat > out"; expected_exit="2"; blacklisted=">" | validation | Q2 (injection >); NP-04; SAD: cli |
| 9 | `test_fr01_03_injection_blacklist` | command="cat < in"; expected_exit="2"; blacklisted="<" | validation | Q2 (injection <); NP-04; SAD: cli |
| 10 | `test_fr01_03_injection_blacklist` | command="echo `pwd`"; expected_exit="2"; blacklisted="`" | validation | Q2 (injection `); NP-04; SAD: cli |
| 11 | `test_fr01_04_duplicate_name_rejection` | name="deploy-prod"; existing_name="deploy-prod"; expected_exit="2"; state_mode="isolate_per_test" | validation | Q2 (duplicate name); NP-04; SAD: store |
| 12 | `test_fr01_05_valid_id_pending_record` | command="echo hi"; expected_status="pending"; expected_id_pattern="^[0-9a-f]{8}$" | happy_path | Q1 (happy path) |
| 13 | `test_fr01_06_atomic_persistence` | command="echo hi"; tmp_orphan_check="true"; subprocess_mode="in_process" | integration | Q5 (fault); NP-07; SAD: store |
| 14 | `test_fr01_07_json_output` | command="echo hi"; json_flag="true"; expected_status="pending" | integration | Q1 + Q7 (--json) |

**Sub-assertions** — predicate over a case's declared Inputs. The predicate
is Python-expression syntax; `check-test-spec-consistency` evaluates it against
the case's concrete inputs. A predicate is `True` for the case it is declared
on, which proves the case faithfully exercises the AC it is derived from. Test
runtime behavior (exit code, status, persistence) is verified by the test
function itself, not by the sub-assertion.

| rule_id | predicate (over Inputs) | applies_to (case #) |
|---|---|---|
| FR01-AC1-empty-string | `len(command) == 0` | 1 |
| FR01-AC1-whitespace-only | `command.strip() == ""` | 2 |
| FR01-AC2-length-gt-1000 | `command_repeat == "x" and command_length == "1001"` | 3 |
| FR01-AC3-blacklist-semicolon | `";" in command` | 4 |
| FR01-AC3-blacklist-pipe | `"\|" in command` | 5 |
| FR01-AC3-blacklist-ampersand | `"&" in command` | 6 |
| FR01-AC3-blacklist-dollar | `"$" in command` | 7 |
| FR01-AC3-blacklist-redirect-out | `">" in command` | 8 |
| FR01-AC3-blacklist-redirect-in | `"<" in command` | 9 |
| FR01-AC3-blacklist-backtick | `` "`" in command `` | 10 |
| FR01-AC4-duplicate-name | `name == existing_name` | 11 |
| FR01-AC5-valid-non-empty | `len(command) > 0` | 12 |
| FR01-AC6-atomic-write | `len(command) > 0` | 13 |
| FR01-AC7-json-mode | `json_flag == "true"` | 14 |

### FR-02: Execute tasks (subprocess, timeout, lifecycle, concurrency)

**Classification**: STATE_MACHINE (task lifecycle) + INTEGRATION (subprocess)
**Active Patterns**: NP-13 (ThreadPoolExecutor + shared lock), NP-15 (subprocess timeout), NP-08 (no shell=True)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr02_01_successful_command_result_fields` | command="echo hi"; expected_status="done"; expected_exit_code="0"; tail_bound="2000" | happy_path | Q1 (success); SAD: executor |
| 2 | `test_fr02_02_nonzero_failure` | command="false"; expected_status="failed"; expected_exit_code="1" | validation | Q2 (non-zero); SAD: executor |
| 3 | `test_fr02_03_timeout_exit_4` | command="sleep 5"; timeout_seconds="1"; expected_status="timeout"; expected_exit_code="4" | validation | Q3 (boundary = timeout); NP-15; SAD: executor |
| 4 | `test_fr02_04_output_tails_2000_chars` | command="python -c 'print(\"x\"*5000)'"; tail_bound="2000" | boundary | Q3 (tail length bound); SAD: executor |
| 5 | `test_fr02_05_no_shell_true_safe_splitting` | source_root="src/taskq"; forbid_token="shell=True" | unit | Q2 (negative assertion); NP-08; SAD: executor |
| 6 | `test_fr02_06_run_all_concurrency` | task_count="8"; max_workers="4"; state_mode="isolate_per_test"; subprocess_mode="in_process" | integration | Q1 + Q4 (state machine); NP-13; SAD: executor+store |
| 7 | `test_fr02_07_thread_safe_lossless_storage` | writer_count="4"; task_count="50"; state_mode="isolate_per_test"; subprocess_mode="in_process" | integration | Q4 + Q5 (concurrent writers); NP-13; SAD: store |

**Sub-assertions**

| rule_id | predicate (over Inputs) | applies_to (case #) |
|---|---|---|
| FR02-AC1-success-command | `len(command) > 0` | 1 |
| FR02-AC2-nonzero-command | `len(command) > 0` | 2 |
| FR02-AC3-timeout-bound | `timeout_seconds == "1"` | 3 |
| FR02-AC4-tail-bound | `tail_bound == "2000"` | 1, 4 |
| FR02-AC5-no-shell-true | `forbid_token == "shell=True"` | 5 |
| FR02-AC6-concurrency-fanout | `max_workers < task_count` | 6 |
| FR02-AC7-writer-count | `writer_count > "1"` | 7 |

### FR-03: Retry and circuit breaker

**Classification**: STATE_MACHINE (CLOSED/OPEN/HALF_OPEN)
**Active Patterns**: NP-13 (cross-process breaker state), NP-15 (timeout participates in retry classification)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr03_01_retry_cap` | command="false"; retry_limit="3"; expected_attempts="3" | boundary | Q1 + Q3 (cap = retry_limit); SAD: executor |
| 2 | `test_fr03_02_exponential_delay_injected_sleep` | retry_index="2"; backoff_base="1.0"; expected_delay="4.0"; sleep_inject="true" | unit | Q1 (deterministic sleep); SAD: executor |
| 3 | `test_fr03_03_threshold_opens_breaker` | consecutive_failures="3"; breaker_threshold="3"; expected_state="OPEN" | happy_path | Q1 + Q4 (state transition); SAD: breaker |
| 4 | `test_fr03_04_open_rejection_no_subprocess` | breaker_state="OPEN"; expected_exit="3"; subprocess_count="0" | validation | Q2 (rejection); SAD: breaker+executor |
| 5 | `test_fr03_05_half_open_success_closes_resets` | breaker_state="HALF_OPEN"; probe_outcome="success"; expected_state="CLOSED"; reset_count="0" | happy_path | Q1 + Q4 (HALF_OPEN → CLOSED); SAD: breaker |
| 6 | `test_fr03_06_half_open_failure_reopens` | breaker_state="HALF_OPEN"; probe_outcome="failure"; expected_state="OPEN" | validation | Q2 + Q4 (HALF_OPEN → OPEN); SAD: breaker |
| 7 | `test_fr03_07_cross_process_persistent_breaker` | process_count="2"; shared_TASKQ_HOME="true"; subprocess_mode="out_of_process"; shared_state="breaker.json" | integration | Q4 + Q5 + Q7 (cross-process); NP-13; SAD: breaker+store |

**Sub-assertions**

| rule_id | predicate (over Inputs) | applies_to (case #) |
|---|---|---|
| FR03-AC1-retry-cap | `len(retry_limit) > 0` | 1 |
| FR03-AC2-exponential-delay | `backoff_base == "1.0" and retry_index == "2" and expected_delay == "4.0"` | 2 |
| FR03-AC3-threshold-met | `consecutive_failures >= breaker_threshold` | 3 |
| FR03-AC4-open-rejects | `breaker_state == "OPEN"` | 4 |
| FR03-AC5-halfopen-success | breaker_state == "HALF_OPEN" and probe_outcome == "success" | 5 |
| FR03-AC6-halfopen-failure | breaker_state == "HALF_OPEN" and probe_outcome == "failure" | 6 |
| FR03-AC7-cross-process | process_count > "1" and shared_TASKQ_HOME == "true" | 7 |

### FR-04: TTL result cache

**Classification**: DATA_ENTITY (cache.json) + INTEGRATION (atomic concurrent)
**Active Patterns**: NP-07 (cache.json atomic write), NP-13 (concurrent readers/writers)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr04_01_sha256_signature` | command="echo hi"; encoding="utf-8" | unit | Q1 (signature determinism); SAD: cache |
| 2 | `test_fr04_02_valid_replay_no_subprocess` | cached_flag="true"; ttl_seconds="60"; age_seconds="10"; cached_attr="true"; subprocess_count="0" | happy_path | Q1 (replay); SAD: executor+cache |
| 3 | `test_fr04_03_missing_entry_executes` | cached_flag="true"; cache_state="missing"; subprocess_count="1" | validation | Q2 (miss path); SAD: executor+cache |
| 4 | `test_fr04_04_expired_entry_executes` | cached_flag="true"; ttl_seconds="60"; age_seconds="120"; cache_state="expired"; subprocess_count="1" | validation | Q2 + Q3 (TTL boundary); SAD: executor+cache |
| 5 | `test_fr04_05_successful_cache_persistence` | command="echo hi"; cache_state="absent"; post_state="present"; subprocess_mode="in_process" | integration | Q1 (write); NP-07; SAD: store+cache |
| 6 | `test_fr04_06_concurrent_cache_atomicity` | reader_count="4"; writer_count="4"; state_mode="isolate_per_test"; subprocess_mode="in_process"; shared_state="cache.json" | integration | Q4 + Q5 (atomic concurrent I/O); NP-07 + NP-13; SAD: store+cache |

**Sub-assertions**

| rule_id | predicate (over Inputs) | applies_to (case #) |
|---|---|---|
| FR04-AC1-signature-input | `len(command) > 0` | 1 |
| FR04-AC2-ttl-valid | `age_seconds < ttl_seconds` | 2 |
| FR04-AC3-ttl-missing | `cache_state == "missing"` | 3 |
| FR04-AC4-ttl-expired | cache_state == "expired" and age_seconds == "120" and ttl_seconds == "60" | 4 |
| FR04-AC5-cache-write | cache_state == "absent" and post_state == "present" | 5 |
| FR04-AC6-concurrent-cache | reader_count > "1" and writer_count > "1" | 6 |

### FR-05: CLI integration (argparse, exit-code map, subcommand dispatch)

**Classification**: API_ENDPOINT (CLI surface) + INTEGRATION (end-to-end subprocess)
**Active Patterns**: NP-04 (validation → exit 2), NP-15 (single-task timeout → exit 4)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr05_01_submit_command` | command="echo hi"; expected_exit="0"; json_flag="false" | happy_path | Q1 (submit dispatches to FR-01); SAD: cli |
| 2 | `test_fr05_02_run_modes_cached` | run_target="--all"; state_mode="isolate_per_test" | integration | Q1 + Q7 (run variants); SAD: cli+executor+cache |
| 3 | `test_fr05_03_status_all_fields` | task_id="abcdef01"; json_flag="false" | integration | Q1 (status fields); SAD: cli+store |
| 4 | `test_fr05_04_list_status_filter` | filter_status="done"; expected_count="1" | integration | Q1 (filter); SAD: cli+store |
| 5 | `test_fr05_05_clear_data` | file_count="3"; state_mode="isolate_per_test" | integration | Q1 (clear); SAD: cli+store |
| 6 | `test_fr05_06_one_line_json` | subcommand="status"; json_flag="true"; line_count="1" | integration | Q1 + Q7 (--json); SAD: cli |
| 7 | `test_fr05_07_exit_code_error_message_map` | scenario="success"; expected_exit="0" | happy_path | Q1 (exit 0); SAD: cli |
| 8 | `test_fr05_07_exit_code_error_message_map` | scenario="validation"; expected_exit="2" | validation | Q2 (exit 2); SAD: cli |
| 9 | `test_fr05_07_exit_code_error_message_map` | scenario="breaker_open"; expected_exit="3" | validation | Q2 (exit 3); SAD: cli |
| 10 | `test_fr05_07_exit_code_error_message_map` | scenario="single_task_timeout"; expected_exit="4" | validation | Q2 (exit 4); NP-15; SAD: cli+executor |
| 11 | `test_fr05_07_exit_code_error_message_map` | scenario="internal_error"; expected_exit="1" | validation | Q2 (exit 1); SAD: cli |
| 12 | `test_fr05_08_python_m_taskq_entry` | entry_args="--help"; expected_exit="0"; state_mode="isolate_per_test" | integration | Q1 (entry point); SAD: __main__+cli |

**Sub-assertions**

| rule_id | predicate (over Inputs) | applies_to (case #) |
|---|---|---|
| FR05-AC1-submit-valid | `len(command) > 0` | 1 |
| FR05-AC2-run-all-target | `run_target == "--all"` | 2 |
| FR05-AC3-status-id | `len(task_id) == 8` | 3 |
| FR05-AC4-list-filter | `len(filter_status) > 0` | 4 |
| FR05-AC5-clear-files | `file_count == "3"` | 5 |
| FR05-AC6-json-line-count | line_count == "1" and json_flag == "true" | 6 |
| FR05-AC7-exit-0 | `expected_exit == "0"` | 7 |
| FR05-AC7-exit-2 | `expected_exit == "2"` | 8 |
| FR05-AC7-exit-3 | `expected_exit == "3"` | 9 |
| FR05-AC7-exit-4 | `expected_exit == "4"` | 10 |
| FR05-AC7-exit-1 | `expected_exit == "1"` | 11 |
| FR05-AC8-entry-args | `len(entry_args) > 0` | 12 |

---

## Cross-Cutting Test Cases

### NFR Integration (TEST_SPEC is the verifier)

Per the NFR Layering Hard Rule, only Integration / system / benchmark layers
are authored here as concrete Inputs + Sub-assertions. Unit / static NFRs are
isolated in the **Deferred to Downstream Phases** table below. Each case
preserves the authoritative `test_name` from `TEST_INVENTORY.yaml`.

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr01_01_100_iteration_p95_benchmark` | iter_count="100"; submit_and_status="combined"; p95_budget_ms="50"; subprocess_mode="in_process"; state_mode="isolate_per_test" | benchmark | Q1 (perf); NP-06; SAD: store |
| 2 | `test_nfr03_02_interrupted_write_json_validity` | file_under_test="tasks.json"; interrupt_kind="SIGKILL"; expected_valid_json="true"; subprocess_mode="in_process"; state_mode="isolate_per_test" | integration | Q5 (fault); NP-07; SAD: store |
| 3 | `test_nfr03_02_interrupted_write_json_validity` | file_under_test="breaker.json"; interrupt_kind="SIGKILL"; expected_valid_json="true"; subprocess_mode="in_process"; state_mode="isolate_per_test" | integration | Q5 (fault); NP-07; SAD: store |
| 4 | `test_nfr03_02_interrupted_write_json_validity` | file_under_test="cache.json"; interrupt_kind="SIGKILL"; expected_valid_json="true"; subprocess_mode="in_process"; state_mode="isolate_per_test" | integration | Q5 (fault); NP-07; SAD: store |
| 5 | `test_nfr03_03_breaker_recovery_bound` | breaker_state="OPEN"; cooldown_seconds="2"; recovery_budget_seconds="3"; state_mode="isolate_per_test" | integration | Q1 (recovery bound); SAD: breaker+store |
| 6 | `test_nfr07_01_tasks_fault_scenarios` | file_under_test="tasks.json"; fault_kind="mid_write_corrupt"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 7 | `test_nfr07_01_tasks_fault_scenarios` | file_under_test="tasks.json"; fault_kind="oserror"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 8 | `test_nfr07_01_tasks_fault_scenarios` | file_under_test="tasks.json"; fault_kind="disk_full"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 9 | `test_nfr07_01_tasks_fault_scenarios` | file_under_test="tasks.json"; fault_kind="kill_during_write"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 10 | `test_nfr07_02_breaker_fault_scenarios` | file_under_test="breaker.json"; fault_kind="mid_write_corrupt"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 11 | `test_nfr07_02_breaker_fault_scenarios` | file_under_test="breaker.json"; fault_kind="oserror"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 12 | `test_nfr07_02_breaker_fault_scenarios` | file_under_test="breaker.json"; fault_kind="disk_full"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 13 | `test_nfr07_02_breaker_fault_scenarios` | file_under_test="breaker.json"; fault_kind="kill_during_write"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 14 | `test_nfr07_03_cache_fault_scenarios` | file_under_test="cache.json"; fault_kind="mid_write_corrupt"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 15 | `test_nfr07_03_cache_fault_scenarios` | file_under_test="cache.json"; fault_kind="oserror"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 16 | `test_nfr07_03_cache_fault_scenarios` | file_under_test="cache.json"; fault_kind="disk_full"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 17 | `test_nfr07_03_cache_fault_scenarios` | file_under_test="cache.json"; fault_kind="kill_during_write"; expected_behavior="recover_or_fail_fast"; subprocess_mode="in_process" | system | Q5 (fault); NP-07; SAD: store+__main__ |
| 18 | `test_nfr07_04_explicit_error_exit_behavior` | file_under_test="tasks.json"; fail_fast_path="true"; expected_exit="1"; subprocess_mode="in_process" | system | Q2 (exit+stderr); SAD: store+cli |
| 19 | `test_nfr08_01_four_process_concurrent_integrity` | process_count="4"; shared_TASKQ_HOME="true"; shared_state="all_three_files"; subprocess_mode="out_of_process"; state_mode="isolate_per_test" | system | Q1 + Q5 (cross-process); NP-13; SAD: store |
| 20 | `test_nfr08_02_exclusive_shared_lock_behavior` | writer_count="1"; reader_count="3"; subprocess_mode="in_process"; state_mode="isolate_per_test" | system | Q1 (lock semantics); NP-13; SAD: store |
| 21 | `test_nfr08_03_network_filesystem_warning_fallback` | fs_kind="network"; warning_token="WARNING"; subprocess_mode="in_process" | system | Q1 (graceful downgrade); SAD: store |
| 22 | `test_nfr09_01_1000_task_p95_benchmark` | task_count="1000"; p95_budget_ms="100"; subprocess_mode="in_process"; state_mode="isolate_per_test" | benchmark | Q1 (scaled perf); NP-06; SAD: store |
| 23 | `test_nfr09_02_100_task_losslessness_json_integrity` | task_count="100"; expected_terminal_states="done"; shared_TASKQ_HOME="true"; subprocess_mode="in_process"; state_mode="isolate_per_test" | system | Q1 (lossless); NP-13; SAD: executor+store |
| 24 | `test_nfr09_03_peak_memory_bound` | task_count="1000"; memory_budget_mb="100"; subprocess_mode="in_process"; state_mode="isolate_per_test" | system | Q1 (memory bound); SAD: store |
| 25 | `test_nfr10_01_v0_to_v1_migration_and_backup` | file_under_test="tasks.json"; source_version="0"; expected_version="1"; backup_suffix=".v0.bak"; subprocess_mode="in_process" | integration | Q1 (migration); SAD: store |
| 26 | `test_nfr10_01_v0_to_v1_migration_and_backup` | file_under_test="breaker.json"; source_version="0"; expected_version="1"; backup_suffix=".v0.bak"; subprocess_mode="in_process" | integration | Q1 (migration); SAD: store |
| 27 | `test_nfr10_01_v0_to_v1_migration_and_backup` | file_under_test="cache.json"; source_version="0"; expected_version="1"; backup_suffix=".v0.bak"; subprocess_mode="in_process" | integration | Q1 (migration); SAD: store |
| 28 | `test_nfr10_02_future_version_refusal` | file_under_test="tasks.json"; source_version="2"; expected_exit="1"; subprocess_mode="in_process" | integration | Q2 (refusal); SAD: store |
| 29 | `test_nfr10_03_failed_migration_preserves_backup_exits_1` | file_under_test="breaker.json"; migration_outcome="failure"; expected_exit="1"; backup_retained="true"; subprocess_mode="in_process" | integration | Q2 (fail-fast); SAD: store |
| 30 | `test_nfr10_04_all_three_file_types` | file_under_test="all_three"; rule_kind="v0_to_v1_migration"; subprocess_mode="in_process" | system | Q1 (uniform across files); SAD: store |

**Sub-assertions** — predicates over the Integration NFR case inputs. They
are evaluated by `check-test-spec-consistency` and must be `True` for the
declared `applies_to` cases.

| rule_id | predicate (over Inputs) | applies_to (case #) |
|---|---|---|
| NFR01-iter-budget | `iter_count == "100"` | 1 |
| NFR01-p95-budget | `p95_budget_ms == "50"` | 1 |
| NFR03-interrupt-tasks | `file_under_test == "tasks.json"` and `interrupt_kind == "SIGKILL"` | 2 |
| NFR03-interrupt-breaker | `file_under_test == "breaker.json"` and `interrupt_kind == "SIGKILL"` | 3 |
| NFR03-interrupt-cache | `file_under_test == "cache.json"` and `interrupt_kind == "SIGKILL"` | 4 |
| NFR03-cooldown-budget | `cooldown_seconds == "2"` and `recovery_budget_seconds == "3"` | 5 |
| NFR07-tasks-mid-write | `file_under_test == "tasks.json"` and `fault_kind == "mid_write_corrupt"` | 6 |
| NFR07-tasks-oserror | `file_under_test == "tasks.json"` and `fault_kind == "oserror"` | 7 |
| NFR07-tasks-disk-full | `file_under_test == "tasks.json"` and `fault_kind == "disk_full"` | 8 |
| NFR07-tasks-kill | `file_under_test == "tasks.json"` and `fault_kind == "kill_during_write"` | 9 |
| NFR07-breaker-mid-write | `file_under_test == "breaker.json"` and `fault_kind == "mid_write_corrupt"` | 10 |
| NFR07-breaker-oserror | `file_under_test == "breaker.json"` and `fault_kind == "oserror"` | 11 |
| NFR07-breaker-disk-full | `file_under_test == "breaker.json"` and `fault_kind == "disk_full"` | 12 |
| NFR07-breaker-kill | `file_under_test == "breaker.json"` and `fault_kind == "kill_during_write"` | 13 |
| NFR07-cache-mid-write | `file_under_test == "cache.json"` and `fault_kind == "mid_write_corrupt"` | 14 |
| NFR07-cache-oserror | `file_under_test == "cache.json"` and `fault_kind == "oserror"` | 15 |
| NFR07-cache-disk-full | `file_under_test == "cache.json"` and `fault_kind == "disk_full"` | 16 |
| NFR07-cache-kill | `file_under_test == "cache.json"` and `fault_kind == "kill_during_write"` | 17 |
| NFR07-fail-fast | `fail_fast_path == "true"` and `expected_exit == "1"` | 18 |
| NFR08-four-process | `process_count == "4"` and `shared_TASKQ_HOME == "true"` | 19 |
| NFR08-lock-mix | `writer_count == "1"` and `reader_count == "3"` | 20 |
| NFR08-nfs-warning | `fs_kind == "network"` and `warning_token == "WARNING"` | 21 |
| NFR09-1000-p95 | `task_count == "1000"` and `p95_budget_ms == "100"` | 22 |
| NFR09-100-lossless | `task_count == "100"` and `expected_terminal_states == "done"` | 23 |
| NFR09-memory-budget | `task_count == "1000"` and `memory_budget_mb == "100"` | 24 |
| NFR10-tasks-migration | `file_under_test == "tasks.json"` and `source_version < expected_version` | 25 |
| NFR10-breaker-migration | `file_under_test == "breaker.json"` and `source_version < expected_version` | 26 |
| NFR10-cache-migration | `file_under_test == "cache.json"` and `source_version < expected_version` | 27 |
| NFR10-future-refusal | `source_version > expected_version` | 28 |
| NFR10-fail-fast-migration | `migration_outcome == "failure"` and `expected_exit == "1"` | 29 |
| NFR10-uniform-files | `file_under_test == "all_three"` and `rule_kind == "v0_to_v1_migration"` | 30 |

### Backward Compatibility

The project is a single-version delivery (no v1→v2 migration path in scope per
NFR-10 future-version refusal). No backward-compatibility tests are required.

### Deployment Smoke

Out of scope for this catalog. The TC-DEPLOY-01 contract lives in P3
`tests/smoke/` outside the per-FR test case enumeration, and the test name
is therefore not present in `TEST_INVENTORY.yaml` §1 (the 63 tc_ids enumerated
in `TRACEABILITY_MATRIX.md` §5 each become one row in this catalog — a 64th
test without an inventory tc_id would violate the catalog contract).

---

## Deferred to Downstream Phases

Per the NFR Layering Hard Rule, all Unit / static NFR test functions are
deferred — they will be authored in their respective layers
(`tests/unit/`, `tests/static/`) by the downstream phase that owns that
verification surface. Each row preserves the authoritative `test_name` from
`TEST_INVENTORY.yaml`; the layer column is a hard contract on the downstream
phase, not a recommendation.

| # | Test Function | NFR | Layer | Title |
|---|---|---|---|---|
| 1 | `test_nfr02_01_source_scan_no_shell_true` | NFR-02 | unit | Source scan finds zero `shell=True` in `src/taskq` |
| 2 | `test_nfr02_02_blacklist_character_coverage` | NFR-02 | unit | Blacklist character coverage for every FR-01 character |
| 3 | `test_nfr03_01_atomic_write_inspection` | NFR-03 | unit | Atomic write helper inspection for all three files |
| 4 | `test_nfr04_01_matching_line_stdout_redaction` | NFR-04 | unit | `stdout_tail` secret-line redaction |
| 5 | `test_nfr04_02_matching_line_stderr_redaction` | NFR-04 | unit | `stderr_tail` secret-line redaction |
| 6 | `test_nfr04_03_non_matching_line_preservation` | NFR-04 | unit | Non-matching-line preservation (no false positives) |
| 7 | `test_nfr05_01_public_docstring_fr_reference` | NFR-05 | unit | Public callable docstring `[FR-XX]` coverage |
| 8 | `test_nfr06_01_all_default_values` | NFR-06 | unit | All 8 `TASKQ_*` canonical defaults |
| 9 | `test_nfr06_02_all_environment_overrides` | NFR-06 | unit | All 8 `TASKQ_*` env-override parametrization |
| 10 | `test_nfr06_03_env_example_eight_variables` | NFR-06 | unit | `.env.example` declares and annotates all 8 vars |
| 11 | `test_nfr07_05_normal_path_disables_injection` | NFR-07 | unit | Normal execution path does not enable fault injection |

---

## Summary

| Metric | Count |
|---|---|
| FRs covered | 5 (FR-01..FR-05) |
| NFRs covered (TEST_SPEC verifier) | 8 (NFR-01, NFR-03, NFR-07, NFR-08, NFR-09, NFR-10) |
| NFRs deferred to downstream phases | 4 (NFR-02, NFR-04, NFR-05, NFR-06) |
| Total catalog rows (FR + NFR Integration) | 76 (46 FR + 30 NFR) |
| Total catalog rows including deferred | 87 (76 + 11 deferred) |
| Unique test functions (FR) | 35 (7+7+7+6+8) |
| Unique test functions (NFR Integration) | 17 |
| Unique test functions (deferred) | 11 |
| Unique test functions total | 63 (35+17+11; matches TEST_INVENTORY.yaml coverage_summary) |
| Catalog rows by type: happy_path | 8 (FR-01×2 + FR-02×1 + FR-03×2 + FR-04×1 + FR-05×2) |
| Catalog rows by type: validation/failure | 20 (FR-01×10 + FR-02×2 + FR-03×2 + FR-04×2 + FR-05×4) |
| Catalog rows by type: boundary | 2 (FR-02×1 + FR-03×1) |
| Catalog rows by type: unit | 3 (FR-02×1 + FR-03×1 + FR-04×1) |
| Catalog rows by type: integration | 13 (FR-01×2 + FR-02×2 + FR-03×1 + FR-04×2 + FR-05×6) |
| Catalog rows by type: system (cross-cutting only) | 12 (NFR07×4 + NFR08×3 + NFR09×2 + NFR10×1 + NFR03×1 + NFR07×1) |
| Catalog rows by type: benchmark (cross-cutting only) | 2 (NFR01×1 + NFR09×1) |
| Catalog rows by type: total | 76 |
| Active NFR patterns applied | NP-04, NP-06, NP-07, NP-08, NP-13, NP-15 |

**Properties (Direction B):** no FR has a clean algebraic invariant that
survives the predicate AST whitelist (e.g. cache.get is not referentially
transparent across TTL; breaker state transitions are not point-free;
signature determinism is trivially reflexive and offers no testable
generator strategy). Per the skill's "do NOT force" rule, Direction B is
explicitly not declared for any FR.

---

*Generated by: derive_test_cases.md v1.1 | harness-methodology v2.13.0*
